/* Packages */
import type { ReactNode } from 'react';

/* Type definitions */
type ErrorBoundary = {
	children: ReactNode;
	message: ReactNode;
};

/* Export prop types */
export type ErrorBoundaryProps = ErrorBoundary;
